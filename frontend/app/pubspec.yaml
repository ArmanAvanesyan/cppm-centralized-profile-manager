name: cppm_app
description: CPPM Centralized Professional Profile Manager - Flutter app (iOS, Android, Windows, macOS).
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  design_system:
    path: ../packages/design_system
  flutter_riverpod: ^2.6.1
  go_router: ^14.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
